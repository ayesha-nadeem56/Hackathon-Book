---
description: "Task list for Module 1 — The Robotic Nervous System (ROS 2)"
---

# Tasks: Module 1 — The Robotic Nervous System (ROS 2)

**Input**: Design documents from `/specs/001-module1-ros2/`
**Prerequisites**: plan.md ✅ | spec.md ✅ | research.md ✅ | data-model.md ✅ | contracts/ ✅

**Tests**: No explicit TDD requested — validation tasks (docusaurus build, py_compile, xmllint) serve as acceptance gates.

**Organization**: Tasks grouped by user story for independent implementation and testing of each chapter.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no blocking dependencies)
- **[Story]**: Maps to user story from spec.md — [US1], [US2], [US3]
- All file paths are relative to repository root

## Path Conventions

- Docusaurus app root: `book/`
- Module 1 content: `book/docs/module-1-ros2/`
- GitHub Actions: `.github/workflows/`

---

## Phase 1: Setup (Docusaurus Initialization)

**Purpose**: Scaffold the Docusaurus 3.x project and configure it for GitHub Pages deployment

- [x] T001 Run `npm init docusaurus@latest book classic` from repo root to scaffold Docusaurus 3.x project at `book/` with the classic preset
- [x] T002 Update `book/docusaurus.config.js` — set `title: "AI-Driven Interactive Book"`, `tagline`, `url` (GitHub Pages URL), `baseUrl: "/Hackathon-Book/"`, `organizationName`, `projectName: "Hackathon-Book"`, and add `themes: ['@docusaurus/theme-mermaid']` and `markdown: { mermaid: true }` to enable Mermaid diagram rendering
- [x] T003 [P] Update `book/sidebars.js` to use autogenerated sidebar: `module.exports = { tutorialSidebar: [{ type: 'autogenerated', dirName: '.' }] }` — this enables zero-config sidebar from directory structure
- [x] T004 [P] Create `.github/workflows/deploy.yml` — GitHub Actions workflow triggered on push to `main`; steps: `actions/checkout@v4`, `actions/setup-node@v4` with `node-version: 18`, `cd book && npm ci && npm run build`, deploy `book/build/` to GitHub Pages using `actions/deploy-pages@v4`
- [x] T005 Run `cd book && npm start` from repo root to confirm Docusaurus launches at `localhost:3000` without errors and the default site renders

**Checkpoint**: Docusaurus running locally at localhost:3000

---

## Phase 2: Foundational (Module Structure — Blocks All Chapters)

**Purpose**: Create Module 1 directory structure that all three chapters depend on

**⚠️ CRITICAL**: No chapter work can begin until this phase is complete

- [x] T006 Create `book/docs/module-1-ros2/` directory and `book/docs/module-1-ros2/_category_.json` with content: `{ "label": "Module 1: The Robotic Nervous System", "position": 1, "collapsible": true, "collapsed": false, "description": "ROS 2 as the middleware connecting AI software with robot hardware" }` — this registers Module 1 in the Docusaurus sidebar
- [x] T007 Create `book/docs/module-1-ros2/index.md` — Module 1 landing page with frontmatter (`id: module-1-overview`, `title: "Module 1: The Robotic Nervous System"`, `sidebar_position: 0`, `description: "Overview of ROS 2 as the nervous system of humanoid robots"`, `tags: [ros2, module-1]`) and body sections: (1) introductory paragraph explaining the module's role in the book, (2) "Prerequisites" callout listing Python 3.10+ and basic AI/ML concepts, (3) "What You'll Learn" bulleted list covering Chapter 1 (ROS 2 fundamentals), Chapter 2 (communication model and Python), Chapter 3 (URDF robot description)
- [x] T008 Run `cd book && npm run build` to confirm the empty Module 1 structure builds without errors and the module appears in the sidebar output

**Checkpoint**: Module 1 sidebar section visible; build succeeds with module structure in place

---

## Phase 3: User Story 1 — ROS 2 Fundamentals (Priority: P1) 🎯 MVP

**Goal**: Deliver Chapter 1 — a standalone readable page where a reader with basic Python knowledge learns ROS 2 middleware concepts, architecture, nodes, topics, and sees a real-world humanoid robot example.

**Independent Test**: Navigate to Chapter 1 on the Docusaurus site; confirm all 6 content sections are present; confirm the architecture Mermaid diagram renders; verify a reader can answer "What is a node?", "What is a topic?", and "Why is ROS 2 used?" from the chapter alone.

### Implementation for User Story 1

- [x] T009 [US1] Create `book/docs/module-1-ros2/chapter-1-intro.md` with complete frontmatter: `id: chapter-1-intro`, `title: "Chapter 1 — Introduction to ROS 2 and the Robotic Nervous System"`, `sidebar_position: 1`, `description: "Understand ROS 2 as the middleware that connects AI software with robot hardware in humanoid robotics."`, `tags: [ros2, module-1, middleware]`; add skeleton section headings for all 6 required sections
- [x] T010 [US1] Write `## Learning Objectives` section in `book/docs/module-1-ros2/chapter-1-intro.md` — bulleted list of exactly 4 outcomes: (1) Define middleware and explain its role in connecting software to hardware; (2) Explain why ROS 2 is preferred over custom middleware in modern robotics; (3) Describe the ROS 2 architecture including the DDS transport layer; (4) Explain how nodes communicate using topics and message passing
- [x] T011 [US1] Write `## What is Middleware?` section in `book/docs/module-1-ros2/chapter-1-intro.md` — define middleware as a software layer that sits between applications and hardware, handling communication, abstraction, and standardization; use a humanoid robot analogy (middleware as the nervous system relaying signals between brain and limbs); explain why custom middleware is costly to build and maintain
- [x] T012 [US1] Write `## Why ROS 2?` section in `book/docs/module-1-ros2/chapter-1-intro.md` — compare ROS 2 with custom solutions and ROS 1; list 5 key advantages: (1) DDS-based reliable transport, (2) real-time support, (3) multi-platform (Linux, Windows, macOS), (4) large ecosystem of packages, (5) built-in security (SROS2); mention that ROS 2 Humble LTS is the target version for all examples in this module
- [x] T013 [US1] Write `## ROS 2 Architecture Overview` section in `book/docs/module-1-ros2/chapter-1-intro.md` — include a Mermaid `graph LR` diagram showing the four layers: `AI["AI Application"]` → `ROS2["ROS 2 Middleware (DDS)"]` → `HAL["Hardware Abstraction Layer"]` → `HW["Sensors & Actuators"]`; below the diagram, explain each layer in 2–3 sentences; define DDS (Data Distribution Service) as the underlying publish-subscribe transport
- [x] T014 [US1] Write `## Nodes, Topics, and Message Passing` section in `book/docs/module-1-ros2/chapter-1-intro.md` — define: node (a single-purpose computational process, e.g., a camera driver or motion planner), topic (a named channel for a specific type of data, e.g., `/joint_states`), message (a typed data structure published to a topic); include a Mermaid `sequenceDiagram` showing: `PublisherNode ->> /topic: publish(msg)` and `/topic ->> SubscriberNode: deliver(msg)`; explain that multiple nodes can publish to and subscribe from the same topic
- [x] T015 [US1] Write `## Real-World Example: Humanoid Walking Command Flow` section in `book/docs/module-1-ros2/chapter-1-intro.md` — trace a walking command through a humanoid robot system: (1) `ai_planner_node` publishes a `Twist` message to `/cmd_vel` topic; (2) `gait_controller_node` subscribes to `/cmd_vel`, computes leg trajectories, publishes `JointTrajectory` to `/leg_joint_commands`; (3) `joint_driver_node` subscribes to `/leg_joint_commands` and sends hardware signals to servo motors; describe each node's role in 1–2 sentences
- [x] T016 [US1] Write `## Summary` section in `book/docs/module-1-ros2/chapter-1-intro.md` — Markdown table with exactly 2 columns (Concept | Key Takeaway) and ≥5 rows covering: Middleware, ROS 2, DDS, Node, Topic, Message
- [x] T017 [US1] Run `cd book && npm run build` to verify `chapter-1-intro.md` compiles without errors; manually confirm both Mermaid diagrams render in browser via `npm start`; verify all 6 terms (middleware, DDS, node, topic, message, ROS 2) are defined in the chapter before they are used in examples

**Checkpoint**: Chapter 1 fully functional — readable independently, all diagrams render, all terms defined

---

## Phase 4: User Story 2 — ROS 2 Communication Model (Priority: P2)

**Goal**: Deliver Chapter 2 — a page where a developer learns to implement ROS 2 publishers, subscribers, and service calls using `rclpy`, with working Python code examples and an explanation of AI-to-hardware message flow.

**Independent Test**: Navigate to Chapter 2; confirm the Mermaid sequence diagram renders; extract both Python code blocks, save as `.py` files, and run `python -m py_compile` on each — both MUST pass; confirm the prerequisite callout links back to Chapter 1.

### Implementation for User Story 2

- [x] T018 [US2] Create `book/docs/module-1-ros2/chapter-2-communication.md` with complete frontmatter: `id: chapter-2-communication`, `title: "Chapter 2 — ROS 2 Communication Model"`, `sidebar_position: 2`, `description: "Implement ROS 2 publishers, subscribers, and services using Python rclpy, and understand how AI agents communicate with robot controllers."`, `tags: [ros2, module-1, rclpy, python]`; add a Docusaurus `:::note` admonition immediately after the title: "**Prerequisites**: This chapter builds on Chapter 1 concepts — nodes, topics, and message passing. [Review Chapter 1](./chapter-1-intro) if needed."
- [x] T019 [US2] Write `## Learning Objectives` and `## Understanding Nodes` sections in `book/docs/module-1-ros2/chapter-2-communication.md` — Learning Objectives: 4 bullets (implement a publisher and subscriber node in Python, explain the difference between topics and services, trace an AI decision as a ROS 2 message to a hardware controller, understand node naming and namespaces); Understanding Nodes: explain node as a Python class inheriting from `rclpy.node.Node`; describe node naming convention (`/namespace/node_name`); explain that nodes register with the ROS 2 graph on startup and are discoverable by other nodes
- [x] T020 [US2] Write `## Topics: Publishers and Subscribers` section in `book/docs/module-1-ros2/chapter-2-communication.md` — include Mermaid `sequenceDiagram` with participants `AI_Agent`, `ROS2_Topic`, `Controller`; show `AI_Agent ->> ROS2_Topic: publish(JointCommand)`, `ROS2_Topic ->> Controller: callback(JointCommand)`, `Controller ->> Actuator: send_hardware_cmd()`; explain that topics are asynchronous (publisher does not wait for subscriber); introduce QoS concept at high level (reliable vs best-effort)
- [x] T021 [P] [US2] Write the publisher node Python code block in `book/docs/module-1-ros2/chapter-2-communication.md` under the `## Topics: Publishers and Subscribers` section — fenced block with `title="publisher_node.py" showLineNumbers`; content: `# ROS 2 Humble — rclpy publisher example` (line 1); import `rclpy`, `rclpy.node.Node`, `std_msgs.msg.String`; define `MinimalPublisher(Node)` class with `__init__` creating a publisher to topic `'robot_status'` with queue size 10 and a 1-second timer; timer callback creates and publishes a `String` message with incrementing counter; `main()` function with `rclpy.init()`, node spin, `rclpy.shutdown()`; every logical step has an inline comment
- [x] T022 [P] [US2] Write the subscriber node Python code block in `book/docs/module-1-ros2/chapter-2-communication.md` under the `## Topics: Publishers and Subscribers` section — fenced block with `title="subscriber_node.py" showLineNumbers`; content: `# ROS 2 Humble — rclpy subscriber example` (line 1); import `rclpy`, `rclpy.node.Node`, `std_msgs.msg.String`; define `MinimalSubscriber(Node)` class with `__init__` creating a subscription to `'robot_status'` topic; `listener_callback` method logging the received message with `self.get_logger().info()`; `main()` with `rclpy.init()`, spin, shutdown; every logical step has an inline comment
- [x] T023 [US2] Write `## Services: Request-Response Communication` section in `book/docs/module-1-ros2/chapter-2-communication.md` — contrast services (synchronous request-response) with topics (asynchronous fire-and-forget); explain when to use each (services for queries with expected responses, e.g., "get current joint positions"; topics for streaming data, e.g., sensor readings); include Mermaid `sequenceDiagram` showing: `AIAgent ->> ServiceServer: Request(get_joint_positions)`, `ServiceServer -->> AIAgent: Response(positions)` with a `Note over ServiceServer: Blocking until response sent` annotation
- [x] T024 [US2] Write `## AI Agent and ROS Controller Interaction` section in `book/docs/module-1-ros2/chapter-2-communication.md` — walk through a concrete example: an AI agent decides "extend left arm to 45°"; it encodes this as a `JointTrajectory` message with joint name `left_shoulder` and target position `0.785` radians; publishes to `/arm_controller/joint_trajectory` topic; `arm_controller_node` subscribes, validates the command, and sends PWM signal to the shoulder servo; explain the separation of concerns: AI layer handles reasoning, ROS layer handles communication, hardware driver handles execution
- [x] T025 [US2] Write `## Summary` section in `book/docs/module-1-ros2/chapter-2-communication.md` — table with ≥5 rows covering: Publisher | Subscriber | Topic | Service | rclpy | QoS — each with a one-line key takeaway
- [x] T026 [US2] Extract both Python code blocks from `chapter-2-communication.md` into temp files `publisher_node.py` and `subscriber_node.py`; run `python -m py_compile publisher_node.py` and `python -m py_compile subscriber_node.py` — both MUST exit with code 0; run `cd book && npm run build` to confirm chapter renders and all Mermaid diagrams compile without errors; delete temp `.py` files after validation

**Checkpoint**: Chapter 2 complete — Python examples pass py_compile, sequence diagrams render, AI-ROS interaction explained

---

## Phase 5: User Story 3 — URDF Robot Description (Priority: P3)

**Goal**: Deliver Chapter 3 — a page where a reader learns the URDF format, can read a humanoid robot URDF file identifying links and joints, and understands how URDF integrates with the ROS 2 runtime.

**Independent Test**: Navigate to Chapter 3; confirm Mermaid link hierarchy diagram renders; extract the URDF code block, save as `humanoid_robot.urdf`, run `xmllint --noout humanoid_robot.urdf` — MUST exit with code 0; confirm at least 7 links and 6 joints are named in the file.

### Implementation for User Story 3

- [x] T027 [US3] Create `book/docs/module-1-ros2/chapter-3-urdf.md` with complete frontmatter: `id: chapter-3-urdf`, `title: "Chapter 3 — Robot Structure and Description (URDF)"`, `sidebar_position: 3`, `description: "Understand how humanoid robots are described in URDF and how robot_state_publisher integrates this with the ROS 2 ecosystem."`, `tags: [ros2, module-1, urdf, humanoid]`; add a `:::note` admonition: "**Prerequisites**: This chapter assumes familiarity with ROS 2 nodes and topics from [Chapter 1](./chapter-1-intro) and [Chapter 2](./chapter-2-communication)."
- [x] T028 [US3] Write `## Learning Objectives` and `## What is URDF?` sections in `book/docs/module-1-ros2/chapter-3-urdf.md` — Learning Objectives: 4 bullets (define URDF and explain its purpose in robotics, identify and interpret `<link>` and `<joint>` elements, read a humanoid robot URDF and trace its link hierarchy, explain how `robot_state_publisher` uses URDF in ROS 2); What is URDF: define URDF as an XML-based format (Unified Robot Description Format) that specifies a robot's geometry, kinematics, and visual appearance; explain it serves as the robot's "blueprint" used by simulation, visualization (RViz), and motion planning tools; note that URDF is standard in ROS 2 and loaded at runtime
- [x] T029 [US3] Write `## Links and Joints` section in `book/docs/module-1-ros2/chapter-3-urdf.md` — define `<link>` as a rigid body segment with sub-elements: `<visual>` (appearance), `<collision>` (collision geometry), `<inertial>` (mass properties); define `<joint>` as a connection between two links specifying: `type`, `<parent link="...">`, `<child link="...">`, `<origin xyz="..." rpy="..."/>`; cover all 4 joint types with description and humanoid body example: `fixed` (skull to head), `revolute` (shoulder, rotation with min/max limits), `continuous` (wheel, unlimited rotation), `prismatic` (spine extension, linear motion)
- [x] T030 [US3] Write `## Humanoid Robot Representation` section in `book/docs/module-1-ros2/chapter-3-urdf.md` — explain how a humanoid body decomposes into links: `base_link` (pelvis/root), `torso`, `head`, `left_arm`, `right_arm`, `left_leg`, `right_leg`; describe appropriate joint types for each connection (pelvis-torso: revolute; torso-head: revolute; torso-left_arm: revolute; torso-right_arm: revolute; torso-left_leg: revolute; torso-right_leg: revolute); include a Mermaid `graph TD` diagram showing the link tree: `base_link --> torso`, `torso --> head`, `torso --> left_arm`, `torso --> right_arm`, `torso --> left_leg`, `torso --> right_leg`
- [x] T031 [US3] Write `## Sample URDF: Simplified Humanoid` section in `book/docs/module-1-ros2/chapter-3-urdf.md` — complete XML code block with `title="humanoid_robot.urdf" showLineNumbers`; structure: `<?xml version="1.0"?>` declaration, `<robot name="simple_humanoid">` root, 7 `<link>` elements (base_link, torso, head, left_arm, right_arm, left_leg, right_leg) each with a `<visual>/<geometry>/<box size="..."/>` to represent body segment dimensions, 6 `<joint>` elements (pelvis_to_torso, torso_to_head, torso_to_left_arm, torso_to_right_arm, torso_to_left_leg, torso_to_right_leg) each with `type="revolute"`, correct parent/child link names, `<origin xyz="..." rpy="0 0 0"/>` with realistic offsets, and `<limit effort="..." velocity="..." lower="-1.57" upper="1.57"/>` for revolute joints; every section has XML comment explaining its purpose
- [x] T032 [US3] Write `## Integration with ROS 2` section in `book/docs/module-1-ros2/chapter-3-urdf.md` — explain `robot_state_publisher` node: reads URDF from the `robot_description` parameter, listens to `/joint_states` topic, publishes TF2 coordinate frame transforms for every link; explain `joint_state_publisher`: simulates joint positions for visualization when no hardware is connected; explain TF2: a library that tracks coordinate frames over time, enabling nodes to query "where is the left hand relative to the torso right now?"; show a `bash` code block (`title="launch_robot_description.bash"`) with the command to load a URDF into `robot_state_publisher` using a launch file snippet
- [x] T033 [US3] Write `## Summary` section in `book/docs/module-1-ros2/chapter-3-urdf.md` — table with ≥5 rows covering: URDF | Link | Joint | robot_state_publisher | TF2 | joint_state_publisher — each with a one-line key takeaway
- [x] T034 [US3] Extract the URDF code block from `chapter-3-urdf.md` into temp file `humanoid_robot.urdf`; run `xmllint --noout humanoid_robot.urdf` — MUST exit with code 0; confirm file contains ≥7 `<link>` and ≥6 `<joint>` elements; run `cd book && npm run build` to confirm chapter renders, Mermaid link hierarchy diagram compiles, and build succeeds; delete temp `.urdf` file after validation

**Checkpoint**: Chapter 3 complete — URDF sample passes xmllint, hierarchy diagram renders, ROS 2 integration explained

---

## Phase 6: Polish & Cross-Cutting Concerns

**Purpose**: Final quality pass across all three chapters and deployment verification

- [x] T035 [P] Review all three chapter files (`chapter-1-intro.md`, `chapter-2-communication.md`, `chapter-3-urdf.md`) in `book/docs/module-1-ros2/` for jargon-free language — verify every technical term (middleware, DDS, node, topic, message, publisher, subscriber, service, rclpy, URDF, link, joint, TF2, robot_state_publisher) is defined in the chapter where it first appears; add inline definitions where missing
- [x] T036 [P] Run `cd book && npm start` and navigate the sidebar — confirm Module 1 appears at position 1, Chapter 1 appears before Chapter 2 which appears before Chapter 3; confirm all internal cross-links (prerequisite callouts in Chapters 2 and 3 linking back to earlier chapters) resolve without 404
- [x] T037 Run `cd book && npm run build` with all three chapters and module index page in place — verify exit code 0 with zero build errors and zero broken-link warnings; save build log output for review
- [x] T038 [P] Verify `.github/workflows/deploy.yml` is syntactically valid YAML; confirm `baseUrl` in `book/docusaurus.config.js` matches the repository name `/Hackathon-Book/`; confirm the workflow `on.push.branches` trigger includes `main`; confirm the build step uses `cd book && npm ci && npm run build` (not `npm install`)
- [x] T039 Validate all three chapters against the 12-item checklist in `specs/001-module1-ros2/contracts/chapter-content-contract.md` — for each chapter confirm: (1) 5 frontmatter fields present, (2) Learning Objectives is first section, (3) Summary with table is last section, (4) ≥1 Mermaid diagram, (5) Python py_compile pass, (6) URDF xmllint pass, (7) all code blocks have `title` attribute, (8) no TODO/placeholder text, (9) all terms defined on first use, (10) prerequisite callouts link to prior chapters, (11) unique sidebar_position, (12) docusaurus build succeeds

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: No dependencies — start immediately
- **Foundational (Phase 2)**: Depends on Phase 1 completion — BLOCKS all chapters
- **US1 Chapter 1 (Phase 3)**: Depends on Phase 2 — no dependency on US2 or US3
- **US2 Chapter 2 (Phase 4)**: Depends on Phase 2 — references Chapter 1 via links but is independently buildable
- **US3 Chapter 3 (Phase 5)**: Depends on Phase 2 — references Chapters 1 & 2 via links but is independently buildable
- **Polish (Phase 6)**: Depends on all three chapters being complete

### User Story Dependencies

- **US1 (P1)**: Can start after Phase 2 — no dependency on US2 or US3
- **US2 (P2)**: Can start after Phase 2 — prerequisite callout links to US1 but Chapter 2 builds independently
- **US3 (P3)**: Can start after Phase 2 — prerequisite callout links to US1/US2 but Chapter 3 builds independently

### Within Each User Story

- File creation (T009 / T018 / T027) MUST come first
- Learning Objectives and first content section follow file creation
- Diagrams integrated within the relevant content section task
- Code examples (Python, URDF) embedded within their section task; [P] only where sections are truly independent
- Validation task (T017 / T026 / T034) is always last in each story phase

### Parallel Opportunities

- T003 + T004 (Phase 1): GitHub Actions workflow and sidebars.js can be authored in parallel
- T021 + T022 (Phase 4): Publisher node and subscriber node Python examples are independent code blocks
- T035 + T036 + T038 (Phase 6): Language review, sidebar check, and workflow verification are independent

---

## Parallel Example: User Story 2

```text
# These two tasks can run in parallel (different content blocks, no ordering dependency):
T021 [P] [US2] Write publisher_node.py code block in chapter-2-communication.md
T022 [P] [US2] Write subscriber_node.py code block in chapter-2-communication.md

# These Polish tasks can run in parallel (different files/checks):
T035 [P] Review all chapters for jargon-free language definitions
T036 [P] Verify sidebar ordering and cross-links via npm start
T038 [P] Verify deploy.yml and docusaurus.config.js baseUrl
```

---

## Implementation Strategy

### MVP First (User Story 1 Only)

1. Complete Phase 1: Setup (T001–T005)
2. Complete Phase 2: Foundational (T006–T008) — CRITICAL, blocks all chapters
3. Complete Phase 3: Chapter 1 (T009–T017)
4. **STOP and VALIDATE**: Chapter 1 is live on localhost:3000 and builds cleanly
5. Deploy to GitHub Pages if MVP demo needed

### Incremental Delivery

1. Setup + Foundational → Docusaurus running with Module 1 sidebar entry
2. Add Chapter 1 → Test independently → Deploy/Demo (MVP: readers can learn ROS 2 fundamentals)
3. Add Chapter 2 → Test independently → Deploy/Demo (adds Python examples)
4. Add Chapter 3 → Test independently → Deploy/Demo (adds URDF content)
5. Polish → Full module complete and validated against content contracts

### Single-Developer Sequence

```text
T001 → T002 → T003+T004(parallel) → T005
→ T006 → T007 → T008
→ T009 → T010 → T011 → T012 → T013 → T014 → T015 → T016 → T017
→ T018 → T019 → T020 → T021+T022(parallel) → T023 → T024 → T025 → T026
→ T027 → T028 → T029 → T030 → T031 → T032 → T033 → T034
→ T035+T036+T038(parallel) → T037 → T039
```

---

## Notes

- `[P]` tasks = different files or independent content blocks — safe to parallelize
- `[Story]` label maps each task to its user story for traceability
- Each chapter is independently deployable — readers can access it at any stage
- Validation tasks (T017, T026, T034, T037) act as acceptance gates — do not skip
- Commit after each checkpoint (end of Phase 2, end of each chapter phase)
- `docusaurus build` is the primary quality gate — it catches broken links, Mermaid errors, and MDX syntax issues simultaneously
